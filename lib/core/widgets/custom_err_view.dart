import 'package:flutter/material.dart';

class CustomErrView extends StatelessWidget {
  const CustomErrView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Oops! , there was error'),);
  }
}